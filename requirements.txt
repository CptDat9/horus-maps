aio-pika==9.6.2
aiosqlite==0.20.0  # async SQLite driver for DB-backed integration tests
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
asyncpg==0.31.0
attrs==26.1.0
brotli==1.2.0
brotli-asgi==1.6.0
buildpg==0.4
certifi==2026.5.20
click==8.4.1
cql2==0.5.6
dotenv==0.9.9
fastapi==0.136.3
GeoAlchemy2==0.20.0
geojson-pydantic==2.1.1
greenlet==3.5.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
hydraters==0.1.3
idna==3.16
iso8601==2.1.0
json-merge-patch==0.3.0
jsonpatch==1.33
jsonpointer==3.1.1
orjson==3.11.9
packaging==26.2
pydantic==2.13.4
pydantic-settings==2.14.1
pydantic_core==2.46.4
pytest==9.0.3
pytest-asyncio==1.4.0
python-dotenv==1.2.2
redis==7.4.0
SQLAlchemy==2.0.50
stac-fastapi-api==6.2.1
stac-fastapi-extensions==6.2.1
stac-fastapi-pgstac==6.2.2
stac-fastapi-types==6.2.1
stac-pydantic==3.5.1
starlette==1.1.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.48.0
websockets==16.0

# ---- Migrations (Alembic) ----
alembic==1.14.0
# Alembic runs synchronously; asyncpg is async-only, so a sync driver is needed.
psycopg2-binary==2.9.10

# ---- AI object detection (YOLO-OBB) — shared venv/image with the backend ----
# Powers app/services/ml_service.py + the `detection` worker task.
# The CPU-only torch wheel keeps the image small (~800MB vs ~5GB with CUDA);
# the extra index serves the +cpu builds.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0
torchvision==0.27.0
ultralytics>=8.3,<9        # YOLO11 / YOLO-OBB inference (model: ml/yolo11s-obb.pt)
# mercantile>=1.2          # not required — slippy-map math is inlined in ml_service
